# Secrets — never commit
.env
.env.example

# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs / tooling
.venv/
venv/
.uv/
.pytest_cache/
.ruff_cache/

# Editors / OS
.vscode/
.idea/
.DS_Store

# Committed as a stray gitlink with no .gitmodules entry (fresh clones get
# an empty dir git refuses to populate); not a submodule, plain ignore.
examples/

# Ignored md files
HISTORICAL_DATA_INVESTIGATION.md

# Bulk/local data (populate separately; never commit large data files)
data/
!data/**/README.md
!data/**/PROVENANCE.json
!data/**/.gitkeep

# Derived from the sealed archive; regenerate with competition_history.write_snapshot.
# Kept out of git (bulk archive-derived data) but still mounted into the Modal
# image, which reads the working tree (add_local_python_source ignore=[]).
src/explaining_markets/artifacts/company_history_snapshot_v1.json
