# data/processed

Reserved for cached, derived research artifacts (e.g. an exported feature
matrix or a `backtest.py` result) built from `data/historical/`. Nothing in
this repository writes here automatically yet — this directory exists so a
future script has a conventional place to put processed outputs without
polluting `data/historical/`'s raw archive cache.

Gitignored except for this README and `.gitkeep`.
